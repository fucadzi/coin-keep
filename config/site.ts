export type SiteConfig = typeof siteConfig;

export const siteConfig = {
    name: 'CoinKeep',
    description: 'View available funds.',
    navItems: [
        {
            label: 'About',
            href: '/about',
        },
    ],
    navMenuItems: [
        {
            label: 'About',
            href: '/about',
        },
    ],
};
